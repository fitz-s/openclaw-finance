# OPEN_QUESTIONS

- Should future handoff zips include sanitized reviewer packets by default?
- Should `build_handoff_zip.py` grow a finance-specific profile that includes selected `docs/openclaw-runtime/*.json` snapshots?
- Should CI enforce placeholder-free handoff docs?
- Should repo-local `.agents/skills` be mirrored into `~/.codex/skills` or left as bundle-only references?
